# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build bikeshop-organizer-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t bikeshop-organizer-api`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=8000

WORKDIR /app

RUN addgroup --system bikeshop-organizer-api && \
  adduser --system -G bikeshop-organizer-api bikeshop-organizer-api

COPY dist/apps/bikeshop-organizer-api ./apps/bikeshop-organizer-api
RUN chown -R bikeshop-organizer-api:bikeshop-organizer-api .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN cd ./apps/bikeshop-organizer-api && npm --omit=dev -f install && npm install pg --save

ENV NODE_ENV="production"


EXPOSE 8000

CMD [ "node", "apps/bikeshop-organizer-api" ]
